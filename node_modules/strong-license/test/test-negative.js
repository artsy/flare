var License = require('../');
var test = require('tap').test;

test('negative license', function(t) {
  var negative = new License();
  var now = new Date();

  t.assert(!negative.covers('*', '*', now), 'empty license does not cover anything');
  t.assert(negative.covers(), 'empty license is still a license');
  t.end();
});
